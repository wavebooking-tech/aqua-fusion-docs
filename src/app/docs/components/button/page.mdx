export const metadata = {
  title: 'Button',
  description: 'Clickable button with multiple visual variants, sizes, and a loading state.',
};

# Button

A versatile button component with support for multiple visual variants, sizes, and a built-in loading spinner. Extends native HTML button attributes and is built with `class-variance-authority` for consistent styling.

## Import

```tsx
import { Button } from '@wavebooking/aqua-fusion';
```

## Preview

<StorybookEmbed story="components-button--default" height={200} />

## Usage

```tsx
<Button variant="default" size="default">
  Click me
</Button>

<Button variant="destructive" loading>
  Deleting...
</Button>

<Button variant="outline" size="lg" disabled>
  Disabled
</Button>
```

## Props

<PropsTable props={[
  { name: 'variant', type: "'default' | 'destructive' | 'outline' | 'subtle' | 'ghost' | 'link'", default: "'default'", description: 'Visual style variant of the button.' },
  { name: 'size', type: "'default' | 'sm' | 'lg' | 'icon'", default: "'default'", description: 'Size of the button. The icon size renders a square button.' },
  { name: 'loading', type: 'boolean', default: 'false', description: 'Whether the button is in a loading state. Displays a spinner and disables interaction.' },
  { name: 'disabled', type: 'boolean', default: 'false', description: 'Whether the button is disabled.' },
  { name: 'children', type: 'ReactNode', description: 'The content rendered inside the button.' },
  { name: 'className', type: 'string', description: 'Additional CSS classes to apply.' },
]} />

The component also accepts all standard HTML `<button>` attributes such as `onClick`, `type`, and `aria-*` props.
