export const metadata = {
  title: 'FileUpload',
  description: 'Drag-and-drop file upload component with image previews and reordering support.',
};

# FileUpload

A drag-and-drop file upload component that displays image previews in a grid. Supports multiple files, configurable file type acceptance, a maximum file limit, and drag-to-reorder functionality.

## Import

```tsx
import { FileUpload } from '@wavebooking/aqua-fusion';
```

## Preview

<StorybookEmbed story="components-fileupload--default" height={300} />

## Usage

```tsx
<FileUpload
  onFilesSelected={(files) => console.log(files)}
  accept="image/*"
  multiple
  maxFiles={8}
/>

<FileUpload
  onFilesSelected={handleFiles}
  accept=".pdf"
  multiple={false}
  maxFiles={1}
/>
```

## Props

<PropsTable props={[
  { name: 'onFilesSelected', type: '(files: (File | string)[]) => void', description: 'Callback fired when files are added, removed, or reordered.', required: true },
  { name: 'accept', type: 'string', default: "'image/*'", description: "Accepted file types (e.g., 'image/*', '.pdf')." },
  { name: 'multiple', type: 'boolean', default: 'true', description: 'Whether multiple files can be uploaded.' },
  { name: 'maxFiles', type: 'number', default: '8', description: 'Maximum number of files allowed.' },
  { name: 'value', type: '(File | string)[]', default: '[]', description: 'Controlled array of current files. Can include File objects or URL strings.' },
  { name: 'className', type: 'string', description: 'Additional CSS classes to apply to the upload area.' },
]} />
