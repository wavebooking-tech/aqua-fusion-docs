# Storybook URL used for live preview embeds and navigation links.
# Set this to the deployed Storybook instance URL (no trailing slash).
# Defaults to http://localhost:6006 when not set.
NEXT_PUBLIC_STORYBOOK_URL=https://storybook.example.com
